# Origin Companion 0.2.6：云端等待问题修复

2026-09-11。状态：修复版已安装，真实云端 Work 合成校准通过；继续作为预览版提供。

## 已确认的问题

已删除的 Work 对话不能再核对完整工具记录。本机保存了两次对应 Beer–Lambert 程序失败：首先依赖未打包的 NumPy，修订后调用不存在的 `GLayer.set_label`。没有可交付的旧图或 OPJU。这与截图的使用内容相符，但不能归因整段 13 分 40 秒，也不能确认 Drive 检索阶段的耗时。

另有可复现的反馈缺陷：失败作业仍带 `poll_after_seconds=3`，进度停在执行中。排查时，Origin Companion 在 ChatGPT 个人创建目录中存在，却没有安装/连接；已恢复同一应用并刷新工具目录。后端版本与云端已登记的工具定义是两项独立检查。

## 修复

- `origin_import_table` 接收已取回的 CSV/TSV 文本，校验并建立不可变快照；相同输入重试复用数据集，避免反复嵌入代码或猜测本机路径。
- 简化配方增加 Beer–Lambert，复用现有 Origin 原生拟合、独立数值核对、OPJU 重开与导出检查。参数要求明确截距、权重和实际单位。
- 失败、取消、中断明确返回 `terminal=true`，不再要求继续查询；保留最后执行阶段并给出对应恢复提示，不自动重跑写操作。
- 工具和技能指导优先使用固定流程，说明运行时依赖及已核实的坐标标签 API。

## 本次实测

| 检查 | 结果 |
|---|---|
| Windows 单元/协议测试 | 125 项通过；Ruff 检查与格式检查通过 |
| 安装包独立运行 | 移除开发运行时 PATH 后，云端文本导入、原生校准和产物检查通过，13.657 秒 |
| 故意缺少模块 | 16.420 秒进入失败终态，返回停止轮询和恢复建议 |
| 真实云端 Work | GPT-5.6 Sol，界面显示“轻度”；输入合成表格至最终回复 111.831 秒 |
| 云端作业 | 排队/执行共 39.371 秒，原生执行/验证 34.313 秒 |
| 产物 | PNG/PDF/SVG/OPJU、拟合 JSON、残差 CSV 均生成；6 个声明产物哈希和大小核对通过，PNG 已由 Agent 视觉审查，OPJU 重开与数据/标签回读通过 |
| 安装及连接 | 本机 0.2.6；同一私人应用重新连接，后台任务运行 0.2.6；其他插件连接未改动 |

合成数据共六点，自由截距、非加权 OLS。斜率 4800.95890410959，截距 0.01973424657534234，R² 0.9999022836181513。光程 1 cm、浓度 mol/dm³ 时，摩尔吸光系数为 4800.9589 L·mol⁻¹·cm⁻¹；A=0.366 对应 7.212429×10⁻⁵ mol/dm³。该数据仅用于工程验收，不是用户实验结论。

经济模式仍显示 5 个工具；完整操作由 13 增至 14。工具定义 JSON 从完整模式 21,837 字节减为 4,908 字节，减少 77.52%；这不是实际计费 token 或模型成功率。安装包约 31.54 MiB，无新增第三方运行时依赖。

失败反馈也经真实 Work 复测：仅读取既有失败作业一次，返回 `failed`、`terminal=true`、无 `poll_after_seconds` 及 API 恢复建议；Work 在约 24.7 秒后报告并停止，没有重跑或继续轮询。该旧失败作业没有恢复产物。

## 边界

本次确认输入表格后的云端使用流程可完成，未重新读取 Drive；原删除任务的全部等待原因仍未知。不能承诺所有模型、所有数据规模、网络状态和 Origin 功能均不再卡住。其他实体电脑、其他模型、其他宿主的自然语言调用以及云端下载按钮仍需各自验收。图和 OPJU 已在本机生成并通过 MCP 预览/资源入口提供，不把资源存在等同于所有宿主都能直接下载。

可复现脚本：[verify_cloud_workflow.py](origin-agent/scripts/verify_cloud_workflow.py)。机器可读记录：[release-0.2.6.json](origin-agent/verification/release-0.2.6.json)。此前复杂功能和 GUI 范围：[详细 Work 验收](WORK_ACCEPTANCE_2026-09-11.md)。
